setdelay: 0.065

var: rounds = 70

open: "https://www.drfrostmaths.com/timestables-game.php"

wait: 3

click: 397, 531

for: i in range(rounds + 1)
    click: 389, 498, 3
    copy
    var: expr = clipboard
    click: 1053, 274
    
    if: len(expr) == 2
        var: result = int(expr[0]) ** int(expr[1])
        type: result
    
    else
        var: toeval = expr.replace("×", "*").replace("÷", "/")
        type: eval(toeval)